site_attachments:
  # Site attachment configuration for global system objects
  - Wales-sdktest:
      syslog_servers:
        - name: syslog-global-test
          interface: GigabitEthernet7/0/0.18
      snmp_servers:
        - snmp-global-test-authpriv
      ipfix_exporters:
        - name: ipfix-global-test
          interface: GigabitEthernet7/0/0.18
  - Liverpool-sdktest:
      snmp_servers:
        - snmp-global-test-authpriv
      ipfix_exporters:
        - name: ipfix-global-test
          interface: GigabitEthernet8/0/0.18