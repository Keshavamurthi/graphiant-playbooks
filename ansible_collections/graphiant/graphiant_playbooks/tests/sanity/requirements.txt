# Dependencies required for ansible-test sanity import tests
# This file is used by ansible-test to install dependencies in its isolated test environment

# Core dependencies for module_utils
PyYAML==6.0.1
Jinja2==3.1.2
future==0.18.3
tabulate==0.9.0

# Graphiant SDK (required for gcsdk_client and related modules)
graphiant-sdk==25.11.1
