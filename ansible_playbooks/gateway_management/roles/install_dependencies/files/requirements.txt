# Python dependencies for the hypervisor virtualenv (/venv).
#
# Ansible runs the gateway modules on the hypervisor through this interpreter, so it needs
# both the libvirt bindings (community.libvirt) and the Graphiant SDK (graphiant.naas).
# Versions are pinned to match ansible_collections/graphiant/naas/_version.py.
lxml==6.0.0
libvirt-python==11.6.0
future==0.18.3
graphiant-sdk>=26.7.0
PyYAML==6.0.1
Jinja2==3.1.6
tabulate==0.9.0
