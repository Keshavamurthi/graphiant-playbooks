{
    "adminStatus": "{{ admin_status | default('true')}}",
    "lan": "{{ lan }}",
    "description": "{{ description | default(lan)}}",
    "alias": "{{ alias | default(lan)}}",
    "ipv4": {
        "address": {
            "address": "{{ ipv4_address }}"
        }
    },
    "ipv6": {
        "address": {
            "address": "{{ ipv6_address }}"
        }
    },
    "maxTransmissionUnit": {{ mtu | default(9000) }}
}
