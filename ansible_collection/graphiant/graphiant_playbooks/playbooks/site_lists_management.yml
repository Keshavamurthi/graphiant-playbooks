---
# Site Lists Management Playbook
# This playbook demonstrates site lists management operations

- name: Site Lists Management Operations
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    # graphiant_host: "https://api.graphiant.com"
    # graphiant_username: "{{ vault_graphiant_username }}"
    # graphiant_password: "{{ vault_graphiant_password }}"

    # Use YAML anchors to avoid repetition
    graphiant_client_params: &graphiant_client_params
      host: "{{ graphiant_host | default(lookup('env', 'GRAPHIANT_HOST')) }}"
      username: "{{ graphiant_username | default(lookup('env', 'GRAPHIANT_USERNAME')) }}"
      password: "{{ graphiant_password | default(lookup('env', 'GRAPHIANT_PASSWORD')) }}"
      
      # host: "{{ lookup('ini', 'url file=/path/to/test.ini section=host') }}"
      # username: "{{ lookup('ini', 'username file=/path/to/test.ini section=credentials') }}"
      # password: "{{ lookup('ini', 'password file=/path/to/test.ini section=credentials') }}"

  tasks:
    - name: Configure Global Site Lists
      graphiant.graphiant_playbooks.graphiant_global_config:
        <<: *graphiant_client_params
        config_file: "sample_global_site_lists.yaml"
        operation: "configure_site_lists"
        detailed_logs: true
        state: present
      register: configure_result
      tags: ['site_lists', 'configure']

    - name: Display Configuration Results
      debug:
        msg: "{{ configure_result.msg }}"

    - name: Deconfigure Global Site Lists
      graphiant.graphiant_playbooks.graphiant_global_config:
        <<: *graphiant_client_params
        config_file: "sample_global_site_lists.yaml"
        operation: "deconfigure_site_lists"
        detailed_logs: true
        state: absent
      register: deconfigure_result
      tags: ['site_lists', 'deconfigure']

    - name: Display Deconfiguration Results
      debug:
        msg: "{{ deconfigure_result.msg }}"
